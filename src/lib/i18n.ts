/**
 * @deprecated This homemade `t()` helper is superseded by `next-intl`.
 * Use `useTranslations()` from `next-intl` in client components or
 * `getTranslations()` from `next-intl/server` in server components.
 *
 * This module is kept as a no-op shim because the runtime cannot
 * remove files in this environment. It exports nothing functional.
 */
export {};
